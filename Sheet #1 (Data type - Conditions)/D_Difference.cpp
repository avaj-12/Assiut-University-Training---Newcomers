#include <bits/stdc++.h>

using namespace std;
int main()
{
    // fast_I_O();
    long long a, b, c, d;
    cin >> a >> b >> c >> d;

    long long ans = (a * b) - (c * d);

    cout << "Difference = " << ans << "\n";

    return 0;
}