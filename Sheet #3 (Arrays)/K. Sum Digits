#include <bits/stdc++.h>

using namespace std;
int main()
{
    int n, sum = 0;
    cin >> n;

    char num[n];
    for (int i = 0; i < n; i++)
    {
        cin >> num[i];
        sum += num[i] - '0';
    }
    cout << sum << "\n";

    return 0;
}
